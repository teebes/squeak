#!/usr/bin/env python

from distutils.core import setup

setup(name='Squeak',
      version='0.1',
      description='Python helper for sqlite3. Allows to drop & alter columns.',
      author="Thibaud Morel l'Horset",
      author_email='teebes@teebes.com',
      url='http://teebes.com/',
      #packages=['blog'],
      py_modules=['squeak']
     )
